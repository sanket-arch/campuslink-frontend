version: "3.8"

services:
  campuslink-frontend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: campuslink-frontend
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
    restart: unless-stopped